#!/usr/bin/env bash

# Find the main PID of Gunicorn using systemctl
main_pid=$(systemctl show -p MainPID --value gunicorn)

# Reload Gunicorn by sending HUP signal to the main process
kill -HUP "$main_pid"
